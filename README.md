# 🧪 음료 배합비 시뮬레이터 (Streamlit App)

음료개발_데이터베이스_v3 기반의 웹 배합비 시뮬레이터입니다.

## 실행 방법

```bash
# 1. 의존성 설치
pip install -r requirements.txt

# 2. 앱 실행
streamlit run app.py
```

브라우저에서 `http://localhost:8501` 자동 오픈됩니다.

## 주요 기능

| 기능 | 설명 |
|------|------|
| 음료유형 + 맛 선택 | 드롭다운 + 직접입력, 가이드 매칭시 🟢표시 |
| AI추천 / 실제사례 가이드 | 보라(AI), 연두(사례) 컬럼으로 참조 |
| 배합비 입력 (100%기준) | 원료DB 드롭다운, 정제수 자동계산 |
| 규격기준 자동판정 | 당도/산도 ✅규격이내 / ⚠️초과 자동표시 |
| 원가계산서 | 원재료비 + 포장재비 + 제조경비 → 원가율 |
| 원료DB 조회 | 174종 원료 검색/필터 |

## 파일 구성

```
beverage_app/
├── app.py                 # 메인 앱
├── beverage_data.json     # DB 데이터 (원료174종, 규격17종, 가이드10조합)
├── requirements.txt       # 의존성
├── .streamlit/
│   └── config.toml        # 테마 설정
└── README.md
```

## 가이드 배합비 조합 (10종)

- 과·채음료: 사과, 딸기, 포도, 오렌지, 복숭아, 망고
- 과·채주스(100%): 사과, 오렌지
- 탄산음료: 레몬
- 유산균음료(살균): 딸기

> 가이드 추가: `beverage_data.json`의 `guides` 항목에 새 조합을 추가하면 됩니다.
